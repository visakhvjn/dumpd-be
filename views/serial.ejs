<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/x-icon" href="/icons/favicon.svg" />
		<title><%= serial.title %> - Dumpd! Series</title>
		<link rel="stylesheet" href="/css/styles.css" />
		<link
			rel="canonical"
			href="https://www.dumpd.in/series/<%= serial.slug %>"
		/>
		<link
			rel="stylesheet"
			href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
		/>
		<%- include('gtag') %>
	</head>
	<body>
		<%- include('header') %>
		<div class="grid grid-cols-1 md:grid-cols-12 gap-2">
			<div class="hidden md:block md:col-span-1"></div>
			<div class="col-span-1 md:col-span-10 grid grid-cols-8">
				<div class="col-span-8 md:col-span-6">
					<div class="col-span-8 pt-8 border-gray-300 mb-8 px-6 md:px-0">
						<time
							><i class="fa-solid fa-calendar"></i><%- serial.createdAt %></time
						>
						<h1><%= serial.title %></h1>
						<p><%- serial.description %></p>
					</div>
					<ul class="">
						<% serial.parts.forEach((part, i) => { %>
						<li class="card">
							<time
								><i class="fa-solid fa-calendar"></i><%= part.createdAt %></time
							>
							<h3>Part <%=i + 1%>: <%= part.title %></h3>
							<p><%- part.description %></p>
							<div>
								<button type="button" class="purple-btn text-sm font-medium">
									<i class="fa-solid fa-file"></i>View Chapter
								</button>
							</div>
						</li>
						<% }) %>
					</ul>
				</div>
				<div class="hidden md:block md:col-span-2"></div>
			</div>
			<div class="hidden md:block col-span-1"></div>
		</div>
	</body>
</html>
