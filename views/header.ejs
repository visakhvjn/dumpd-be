<nav
	class="grid grid-cols-24 py-4 mb-4 sticky top-0 z-50 shadow bg-white dark:bg-black dark:text-zinc-100"
>
	<div class="col-span-2 md:col-span-2">
		<button class="md:hidden text-3xl" id="menu-toggle">☰</button>
	</div>
	<div class="col-span-21 md:col-span-20 flex justify-between items-center">
		<a href="/"
			><span
				class="text-xl md:text-xl font-bold text-white bg-black dark:bg-zinc-800 p-2"
				>Dumpd!</span
			></a
		>
		<% if (!isLoggedIn) { %>
		<a href="/login"><button class="btn">Login</button></a>
		<% } %> <% if (isLoggedIn) { %>
		<div class="flex justify-center items-center gap-2">
			<div class="flex flex-col text-end">
				<span class="text-sm font-bold"><%= authUser.name %></span>
				<a class="hover:underline -mt-1 p-0" href="/logout"
					><span class="text-xs">Logout</span></a
				>
			</div>
			<img class="rounded-full w-10 h-10" src="<%= authUser.picture %>" />
		</div>
		<% } %>
	</div>
	<div class="col-span-1"></div>
</nav>
